import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  title: {
    fontSize: 20,
    fontWeight: 'bold',
    marginTop: 50,
    padding: 20,
  },
  row: {
    flexDirection: 'row',
  },
  titleCell: {
    marginLeft: 20,
    height: 30,
    fontWeight: 'bold',
  },
});

export default styles;
